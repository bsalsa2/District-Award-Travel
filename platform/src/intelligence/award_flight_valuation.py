import numpy as np

def calculate_award_flight_value(passenger_name, flight_number, booking_date):
    # Calculate the award flight value based on the passenger name, flight number, and booking date
    # This is a simplified example and the actual calculation would depend on various factors
    value = np.random.randint(100, 1000)
    return value

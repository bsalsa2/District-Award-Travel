version: '3'
services:
  award-travel:
    build: .
    ports:
      - "80:80"
    deploy:
      replicas: 1
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
      restart_policy:
        condition: on-failure
    environment:
      - DEMAND_DATA_FILE=data.csv

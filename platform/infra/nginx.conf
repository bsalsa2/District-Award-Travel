http {
    server {
        listen 80;
        server_name district-award-travel.com;

        location / {
            proxy_pass http://kubernetes-master:6443;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}
